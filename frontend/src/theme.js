import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#85CED8', // non-photo-blue
    },
    secondary: {
      main: '#7C89A1', // cool-gray
    },
  },
});

export default theme;